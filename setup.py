# setup.py
"""Setup script for the bertuner project."""
import setuptools

setuptools.setup(
    name="bertuner",  # Should match your main package directory
    version="0.1.0",  # Set an initial version
    packages=setuptools.find_packages(),  # Automatically find your 'diabetes_ha1c' package
)
